// Code generated by tools/generate; DO NOT EDIT.

// Copyright Jamf Software LLC 2026
// SPDX-License-Identifier: MIT

package account

import (
	"bytes"
	"encoding/json"
	"testing"
)

// TestConnectionRequestConnection_Marshal asserts that each variant marshals to exactly the bytes
// the variant alone produces — the union adds no wrapper and drops no field —
// that the payload survives a decode into Raw, that setting no variant emits
// null rather than an empty object, and that setting two is refused instead of
// silently shipping one of them.
func TestConnectionRequestConnection_Marshal(t *testing.T) {
	t.Run("OidcConnectionSettings", func(t *testing.T) {
		variant := &OidcConnectionSettings{}
		got, err := json.Marshal(ConnectionRequestConnection{OidcConnectionSettings: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectionRequestConnection
		if err := json.Unmarshal(got, &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if !bytes.Equal(back.Raw, got) {
			t.Fatalf("Raw is %s, want the payload %s", back.Raw, got)
		}
		if err := json.Unmarshal(back.Raw, &OidcConnectionSettings{}); err != nil {
			t.Fatalf("decoding Raw back into OidcConnectionSettings: %v", err)
		}
	})
	t.Run("EntraConnectionSettings", func(t *testing.T) {
		variant := &EntraConnectionSettings{}
		got, err := json.Marshal(ConnectionRequestConnection{EntraConnectionSettings: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectionRequestConnection
		if err := json.Unmarshal(got, &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if !bytes.Equal(back.Raw, got) {
			t.Fatalf("Raw is %s, want the payload %s", back.Raw, got)
		}
		if err := json.Unmarshal(back.Raw, &EntraConnectionSettings{}); err != nil {
			t.Fatalf("decoding Raw back into EntraConnectionSettings: %v", err)
		}
	})
	t.Run("OktaConnectionSettings", func(t *testing.T) {
		variant := &OktaConnectionSettings{}
		got, err := json.Marshal(ConnectionRequestConnection{OktaConnectionSettings: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectionRequestConnection
		if err := json.Unmarshal(got, &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if !bytes.Equal(back.Raw, got) {
			t.Fatalf("Raw is %s, want the payload %s", back.Raw, got)
		}
		if err := json.Unmarshal(back.Raw, &OktaConnectionSettings{}); err != nil {
			t.Fatalf("decoding Raw back into OktaConnectionSettings: %v", err)
		}
	})
	t.Run("GoogleConnectionSettings", func(t *testing.T) {
		variant := &GoogleConnectionSettings{}
		got, err := json.Marshal(ConnectionRequestConnection{GoogleConnectionSettings: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectionRequestConnection
		if err := json.Unmarshal(got, &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if !bytes.Equal(back.Raw, got) {
			t.Fatalf("Raw is %s, want the payload %s", back.Raw, got)
		}
		if err := json.Unmarshal(back.Raw, &GoogleConnectionSettings{}); err != nil {
			t.Fatalf("decoding Raw back into GoogleConnectionSettings: %v", err)
		}
	})
	t.Run("no variant set", func(t *testing.T) {
		got, err := json.Marshal(ConnectionRequestConnection{})
		if err != nil {
			t.Fatalf("marshal: %v", err)
		}
		if string(got) != "null" {
			t.Fatalf("zero union marshalled to %s, want null", got)
		}
	})
	t.Run("two variants set", func(t *testing.T) {
		if _, err := json.Marshal(ConnectionRequestConnection{OidcConnectionSettings: &OidcConnectionSettings{}, EntraConnectionSettings: &EntraConnectionSettings{}}); err == nil {
			t.Fatal("marshalling two variants succeeded; want an error naming both")
		}
	})
}
