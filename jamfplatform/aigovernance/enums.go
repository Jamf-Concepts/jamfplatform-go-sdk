// Code generated by tools/generate; DO NOT EDIT.

// Copyright Jamf Software LLC 2026
// SPDX-License-Identifier: MIT

package aigovernance

// BlueprintDeploymentState is the set of values accepted by BlueprintDeployment.State.
type BlueprintDeploymentState = string

// BlueprintDeploymentState values accepted by the Jamf API. The alias above is a string, so
// these constants pass to any parameter or field declared as a plain string.
const (
	BlueprintDeploymentStateNotDeployed BlueprintDeploymentState = "NOT_DEPLOYED"
	BlueprintDeploymentStateDeployed    BlueprintDeploymentState = "DEPLOYED"
	BlueprintDeploymentStateOutOfDate   BlueprintDeploymentState = "OUT_OF_DATE"
)

// BlueprintDeploymentStateValues returns every value the Jamf API accepts for BlueprintDeploymentState,
// in the order the spec declares them. Returns a fresh slice per call, so no
// caller can corrupt the set for the rest of the process — which a package
// level var would allow. Suits attribute validation (Terraform's
// stringvalidator.OneOf, say) and anything that needs to enumerate the set
// rather than name one member.
func BlueprintDeploymentStateValues() []BlueprintDeploymentState {
	return []BlueprintDeploymentState{
		BlueprintDeploymentStateNotDeployed,
		BlueprintDeploymentStateDeployed,
		BlueprintDeploymentStateOutOfDate,
	}
}

// DeploymentRunState is the set of values accepted by DeploymentRun.State.
type DeploymentRunState = string

// DeploymentRunState values accepted by the Jamf API. The alias above is a string, so
// these constants pass to any parameter or field declared as a plain string.
const (
	DeploymentRunStatePending   DeploymentRunState = "PENDING"
	DeploymentRunStateDeploying DeploymentRunState = "DEPLOYING"
	DeploymentRunStateSucceeded DeploymentRunState = "SUCCEEDED"
	DeploymentRunStateFailed    DeploymentRunState = "FAILED"
)

// DeploymentRunStateValues returns every value the Jamf API accepts for DeploymentRunState,
// in the order the spec declares them. Returns a fresh slice per call, so no
// caller can corrupt the set for the rest of the process — which a package
// level var would allow. Suits attribute validation (Terraform's
// stringvalidator.OneOf, say) and anything that needs to enumerate the set
// rather than name one member.
func DeploymentRunStateValues() []DeploymentRunState {
	return []DeploymentRunState{
		DeploymentRunStatePending,
		DeploymentRunStateDeploying,
		DeploymentRunStateSucceeded,
		DeploymentRunStateFailed,
	}
}

// PolicyDetailStatus is the set of values accepted by PolicyDetail.Status.
type PolicyDetailStatus = string

// PolicyDetailStatus values accepted by the Jamf API. The alias above is a string, so
// these constants pass to any parameter or field declared as a plain string.
const (
	PolicyDetailStatusActive   PolicyDetailStatus = "ACTIVE"
	PolicyDetailStatusArchived PolicyDetailStatus = "ARCHIVED"
)

// PolicyDetailStatusValues returns every value the Jamf API accepts for PolicyDetailStatus,
// in the order the spec declares them. Returns a fresh slice per call, so no
// caller can corrupt the set for the rest of the process — which a package
// level var would allow. Suits attribute validation (Terraform's
// stringvalidator.OneOf, say) and anything that needs to enumerate the set
// rather than name one member.
func PolicyDetailStatusValues() []PolicyDetailStatus {
	return []PolicyDetailStatus{
		PolicyDetailStatusActive,
		PolicyDetailStatusArchived,
	}
}

// PolicySummaryStatus is the set of values accepted by PolicySummary.Status.
type PolicySummaryStatus = string

// PolicySummaryStatus values accepted by the Jamf API. The alias above is a string, so
// these constants pass to any parameter or field declared as a plain string.
const (
	PolicySummaryStatusActive   PolicySummaryStatus = "ACTIVE"
	PolicySummaryStatusArchived PolicySummaryStatus = "ARCHIVED"
)

// PolicySummaryStatusValues returns every value the Jamf API accepts for PolicySummaryStatus,
// in the order the spec declares them. Returns a fresh slice per call, so no
// caller can corrupt the set for the rest of the process — which a package
// level var would allow. Suits attribute validation (Terraform's
// stringvalidator.OneOf, say) and anything that needs to enumerate the set
// rather than name one member.
func PolicySummaryStatusValues() []PolicySummaryStatus {
	return []PolicySummaryStatus{
		PolicySummaryStatusActive,
		PolicySummaryStatusArchived,
	}
}
