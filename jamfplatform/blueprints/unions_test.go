// Code generated by tools/generate; DO NOT EDIT.

// Copyright Jamf Software LLC 2026
// SPDX-License-Identifier: MIT

package blueprints

import (
	"bytes"
	"encoding/json"
	"testing"
)

// TestBookmarkItem_Marshal asserts that each "Type" value marshals to exactly the bytes
// its variant alone produces, that a payload carrying the value decodes into
// that variant, and that an unrecognised value leaves every variant nil while
// preserving the discriminator rather than guessing.
func TestBookmarkItem_Marshal(t *testing.T) {
	t.Run("BOOKMARK", func(t *testing.T) {
		variant := &URLBookmarkItem{}
		got, err := json.Marshal(BookmarkItem{Type: "BOOKMARK", BOOKMARK: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back BookmarkItem
		if err := json.Unmarshal([]byte(`{"Type":"BOOKMARK"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Type != "BOOKMARK" {
			t.Fatalf("Type = %q after decode, want %q", back.Type, "BOOKMARK")
		}
		if back.BOOKMARK == nil {
			t.Fatal("BOOKMARK decoded with BOOKMARK nil — the value is mapped to no variant")
		}
	})
	t.Run("FOLDER", func(t *testing.T) {
		variant := &FolderBookmarkItem{}
		got, err := json.Marshal(BookmarkItem{Type: "FOLDER", FOLDER: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back BookmarkItem
		if err := json.Unmarshal([]byte(`{"Type":"FOLDER"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Type != "FOLDER" {
			t.Fatalf("Type = %q after decode, want %q", back.Type, "FOLDER")
		}
		if back.FOLDER == nil {
			t.Fatal("FOLDER decoded with FOLDER nil — the value is mapped to no variant")
		}
	})
	t.Run("unrecognised discriminator", func(t *testing.T) {
		var back BookmarkItem
		if err := json.Unmarshal([]byte(`{"Type":"NOT_A_Type_VALUE"}`), &back); err != nil {
			t.Fatalf("unmarshal: %v", err)
		}
		if back.Type != "NOT_A_Type_VALUE" {
			t.Fatalf("Type = %q, want the unrecognised value preserved", back.Type)
		}
		if back.BOOKMARK != nil {
			t.Error("BOOKMARK was populated for an unrecognised discriminator")
		}
		if back.FOLDER != nil {
			t.Error("FOLDER was populated for an unrecognised discriminator")
		}
	})
}

// TestSwUpdateAutomaticConfiguration_Marshal asserts that each "strategy" value marshals to exactly the bytes
// its variant alone produces, that a payload carrying the value decodes into
// that variant, and that an unrecognised value leaves every variant nil while
// preserving the discriminator rather than guessing.
func TestSwUpdateAutomaticConfiguration_Marshal(t *testing.T) {
	t.Run("LATEST", func(t *testing.T) {
		variant := &SwUpdateLatestConfiguration{}
		got, err := json.Marshal(SwUpdateAutomaticConfiguration{Strategy: "LATEST", LATEST: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back SwUpdateAutomaticConfiguration
		if err := json.Unmarshal([]byte(`{"strategy":"LATEST"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Strategy != "LATEST" {
			t.Fatalf("Strategy = %q after decode, want %q", back.Strategy, "LATEST")
		}
		if back.LATEST == nil {
			t.Fatal("LATEST decoded with LATEST nil — the value is mapped to no variant")
		}
	})
	t.Run("SEMANTIC", func(t *testing.T) {
		variant := &SwUpdateSemanticConfiguration{}
		got, err := json.Marshal(SwUpdateAutomaticConfiguration{Strategy: "SEMANTIC", SEMANTIC: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back SwUpdateAutomaticConfiguration
		if err := json.Unmarshal([]byte(`{"strategy":"SEMANTIC"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Strategy != "SEMANTIC" {
			t.Fatalf("Strategy = %q after decode, want %q", back.Strategy, "SEMANTIC")
		}
		if back.SEMANTIC == nil {
			t.Fatal("SEMANTIC decoded with SEMANTIC nil — the value is mapped to no variant")
		}
	})
	t.Run("unrecognised discriminator", func(t *testing.T) {
		var back SwUpdateAutomaticConfiguration
		if err := json.Unmarshal([]byte(`{"strategy":"NOT_A_Strategy_VALUE"}`), &back); err != nil {
			t.Fatalf("unmarshal: %v", err)
		}
		if back.Strategy != "NOT_A_Strategy_VALUE" {
			t.Fatalf("Strategy = %q, want the unrecognised value preserved", back.Strategy)
		}
		if back.LATEST != nil {
			t.Error("LATEST was populated for an unrecognised discriminator")
		}
		if back.SEMANTIC != nil {
			t.Error("SEMANTIC was populated for an unrecognised discriminator")
		}
	})
}

// TestSwUpdateConfiguration_Marshal asserts that each "enforcementType" value marshals to exactly the bytes
// its variant alone produces, that a payload carrying the value decodes into
// that variant, and that an unrecognised value leaves every variant nil while
// preserving the discriminator rather than guessing.
func TestSwUpdateConfiguration_Marshal(t *testing.T) {
	t.Run("AUTOMATIC", func(t *testing.T) {
		variant := &SwUpdateAutomaticConfiguration{}
		got, err := json.Marshal(SwUpdateConfiguration{EnforcementType: "AUTOMATIC", AUTOMATIC: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back SwUpdateConfiguration
		if err := json.Unmarshal([]byte(`{"enforcementType":"AUTOMATIC"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.EnforcementType != "AUTOMATIC" {
			t.Fatalf("EnforcementType = %q after decode, want %q", back.EnforcementType, "AUTOMATIC")
		}
		if back.AUTOMATIC == nil {
			t.Fatal("AUTOMATIC decoded with AUTOMATIC nil — the value is mapped to no variant")
		}
	})
	t.Run("MANUAL", func(t *testing.T) {
		variant := &SwUpdateManualConfiguration{}
		got, err := json.Marshal(SwUpdateConfiguration{EnforcementType: "MANUAL", MANUAL: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back SwUpdateConfiguration
		if err := json.Unmarshal([]byte(`{"enforcementType":"MANUAL"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.EnforcementType != "MANUAL" {
			t.Fatalf("EnforcementType = %q after decode, want %q", back.EnforcementType, "MANUAL")
		}
		if back.MANUAL == nil {
			t.Fatal("MANUAL decoded with MANUAL nil — the value is mapped to no variant")
		}
	})
	t.Run("unrecognised discriminator", func(t *testing.T) {
		var back SwUpdateConfiguration
		if err := json.Unmarshal([]byte(`{"enforcementType":"NOT_A_EnforcementType_VALUE"}`), &back); err != nil {
			t.Fatalf("unmarshal: %v", err)
		}
		if back.EnforcementType != "NOT_A_EnforcementType_VALUE" {
			t.Fatalf("EnforcementType = %q, want the unrecognised value preserved", back.EnforcementType)
		}
		if back.AUTOMATIC != nil {
			t.Error("AUTOMATIC was populated for an unrecognised discriminator")
		}
		if back.MANUAL != nil {
			t.Error("MANUAL was populated for an unrecognised discriminator")
		}
	})
}
