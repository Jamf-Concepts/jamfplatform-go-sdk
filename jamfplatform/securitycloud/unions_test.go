// Code generated by tools/generate; DO NOT EDIT.

// Copyright Jamf Software LLC 2026
// SPDX-License-Identifier: MIT

package securitycloud

import (
	"bytes"
	"encoding/json"
	"testing"
)

// TestConnectorCreateRequestBody_Marshal asserts that each "vendor" value marshals to exactly the bytes
// its variant alone produces, that a payload carrying the value decodes into
// that variant, and that an unrecognised value leaves every variant nil while
// preserving the discriminator rather than guessing.
func TestConnectorCreateRequestBody_Marshal(t *testing.T) {
	t.Run("AIRWATCH", func(t *testing.T) {
		variant := &WorkspaceOneConnectorCreateRequest{}
		got, err := json.Marshal(ConnectorCreateRequestBody{Vendor: "AIRWATCH", AIRWATCH: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"AIRWATCH"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Vendor != "AIRWATCH" {
			t.Fatalf("Vendor = %q after decode, want %q", back.Vendor, "AIRWATCH")
		}
		if back.AIRWATCH == nil {
			t.Fatal("AIRWATCH decoded with AIRWATCH nil — the value is mapped to no variant")
		}
	})
	t.Run("GOOGLE", func(t *testing.T) {
		variant := &GoogleConnectorCreateRequest{}
		got, err := json.Marshal(ConnectorCreateRequestBody{Vendor: "GOOGLE", GOOGLE: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"GOOGLE"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Vendor != "GOOGLE" {
			t.Fatalf("Vendor = %q after decode, want %q", back.Vendor, "GOOGLE")
		}
		if back.GOOGLE == nil {
			t.Fatal("GOOGLE decoded with GOOGLE nil — the value is mapped to no variant")
		}
	})
	t.Run("INTUNE", func(t *testing.T) {
		variant := &IntuneConnectorCreateRequest{}
		got, err := json.Marshal(ConnectorCreateRequestBody{Vendor: "INTUNE", INTUNE: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"INTUNE"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Vendor != "INTUNE" {
			t.Fatalf("Vendor = %q after decode, want %q", back.Vendor, "INTUNE")
		}
		if back.INTUNE == nil {
			t.Fatal("INTUNE decoded with INTUNE nil — the value is mapped to no variant")
		}
	})
	t.Run("JAMF_PRO", func(t *testing.T) {
		variant := &JamfProConnectorCreateRequest{}
		got, err := json.Marshal(ConnectorCreateRequestBody{Vendor: "JAMF_PRO", JAMFPRO: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"JAMF_PRO"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Vendor != "JAMF_PRO" {
			t.Fatalf("Vendor = %q after decode, want %q", back.Vendor, "JAMF_PRO")
		}
		if back.JAMFPRO == nil {
			t.Fatal("JAMF_PRO decoded with JAMFPRO nil — the value is mapped to no variant")
		}
	})
	t.Run("JAMF_SCHOOL", func(t *testing.T) {
		variant := &JamfSchoolConnectorCreateRequest{}
		got, err := json.Marshal(ConnectorCreateRequestBody{Vendor: "JAMF_SCHOOL", JAMFSCHOOL: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"JAMF_SCHOOL"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Vendor != "JAMF_SCHOOL" {
			t.Fatalf("Vendor = %q after decode, want %q", back.Vendor, "JAMF_SCHOOL")
		}
		if back.JAMFSCHOOL == nil {
			t.Fatal("JAMF_SCHOOL decoded with JAMFSCHOOL nil — the value is mapped to no variant")
		}
	})
	t.Run("MAAS360", func(t *testing.T) {
		variant := &Maas360ConnectorCreateRequest{}
		got, err := json.Marshal(ConnectorCreateRequestBody{Vendor: "MAAS360", MAAS360: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"MAAS360"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Vendor != "MAAS360" {
			t.Fatalf("Vendor = %q after decode, want %q", back.Vendor, "MAAS360")
		}
		if back.MAAS360 == nil {
			t.Fatal("MAAS360 decoded with MAAS360 nil — the value is mapped to no variant")
		}
	})
	t.Run("MOBILEIRONCLOUD", func(t *testing.T) {
		variant := &MobileIronCloudConnectorCreateRequest{}
		got, err := json.Marshal(ConnectorCreateRequestBody{Vendor: "MOBILEIRONCLOUD", MOBILEIRONCLOUD: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"MOBILEIRONCLOUD"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Vendor != "MOBILEIRONCLOUD" {
			t.Fatalf("Vendor = %q after decode, want %q", back.Vendor, "MOBILEIRONCLOUD")
		}
		if back.MOBILEIRONCLOUD == nil {
			t.Fatal("MOBILEIRONCLOUD decoded with MOBILEIRONCLOUD nil — the value is mapped to no variant")
		}
	})
	t.Run("MOBILEIRONCORE", func(t *testing.T) {
		variant := &MobileIronCoreConnectorCreateRequest{}
		got, err := json.Marshal(ConnectorCreateRequestBody{Vendor: "MOBILEIRONCORE", MOBILEIRONCORE: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"MOBILEIRONCORE"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Vendor != "MOBILEIRONCORE" {
			t.Fatalf("Vendor = %q after decode, want %q", back.Vendor, "MOBILEIRONCORE")
		}
		if back.MOBILEIRONCORE == nil {
			t.Fatal("MOBILEIRONCORE decoded with MOBILEIRONCORE nil — the value is mapped to no variant")
		}
	})
	t.Run("WIZY", func(t *testing.T) {
		variant := &WizyConnectorCreateRequest{}
		got, err := json.Marshal(ConnectorCreateRequestBody{Vendor: "WIZY", WIZY: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"WIZY"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Vendor != "WIZY" {
			t.Fatalf("Vendor = %q after decode, want %q", back.Vendor, "WIZY")
		}
		if back.WIZY == nil {
			t.Fatal("WIZY decoded with WIZY nil — the value is mapped to no variant")
		}
	})
	t.Run("XENMOBILE", func(t *testing.T) {
		variant := &XenMobileConnectorCreateRequest{}
		got, err := json.Marshal(ConnectorCreateRequestBody{Vendor: "XENMOBILE", XENMOBILE: variant})
		if err != nil {
			t.Fatalf("marshal union: %v", err)
		}
		want, err := json.Marshal(variant)
		if err != nil {
			t.Fatalf("marshal variant: %v", err)
		}
		if !bytes.Equal(got, want) {
			t.Fatalf("union marshalled to %s, want the bare variant %s", got, want)
		}
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"XENMOBILE"}`), &back); err != nil {
			t.Fatalf("unmarshal union: %v", err)
		}
		if back.Vendor != "XENMOBILE" {
			t.Fatalf("Vendor = %q after decode, want %q", back.Vendor, "XENMOBILE")
		}
		if back.XENMOBILE == nil {
			t.Fatal("XENMOBILE decoded with XENMOBILE nil — the value is mapped to no variant")
		}
	})
	t.Run("unrecognised discriminator", func(t *testing.T) {
		var back ConnectorCreateRequestBody
		if err := json.Unmarshal([]byte(`{"vendor":"NOT_A_Vendor_VALUE"}`), &back); err != nil {
			t.Fatalf("unmarshal: %v", err)
		}
		if back.Vendor != "NOT_A_Vendor_VALUE" {
			t.Fatalf("Vendor = %q, want the unrecognised value preserved", back.Vendor)
		}
		if back.AIRWATCH != nil {
			t.Error("AIRWATCH was populated for an unrecognised discriminator")
		}
		if back.GOOGLE != nil {
			t.Error("GOOGLE was populated for an unrecognised discriminator")
		}
		if back.INTUNE != nil {
			t.Error("INTUNE was populated for an unrecognised discriminator")
		}
		if back.JAMFPRO != nil {
			t.Error("JAMFPRO was populated for an unrecognised discriminator")
		}
		if back.JAMFSCHOOL != nil {
			t.Error("JAMFSCHOOL was populated for an unrecognised discriminator")
		}
		if back.MAAS360 != nil {
			t.Error("MAAS360 was populated for an unrecognised discriminator")
		}
		if back.MOBILEIRONCLOUD != nil {
			t.Error("MOBILEIRONCLOUD was populated for an unrecognised discriminator")
		}
		if back.MOBILEIRONCORE != nil {
			t.Error("MOBILEIRONCORE was populated for an unrecognised discriminator")
		}
		if back.WIZY != nil {
			t.Error("WIZY was populated for an unrecognised discriminator")
		}
		if back.XENMOBILE != nil {
			t.Error("XENMOBILE was populated for an unrecognised discriminator")
		}
	})
}
